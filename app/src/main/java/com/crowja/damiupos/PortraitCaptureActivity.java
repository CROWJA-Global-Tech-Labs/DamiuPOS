package com.crowja.damiupos;

import com.journeyapps.barcodescanner.CaptureActivity;

/**
 * QR capture screen locked to portrait (orientation fixed in the manifest entry).
 * Used for provisioning scan so the camera preview matches how staff hold the phone.
 */
public class PortraitCaptureActivity extends CaptureActivity {
}
